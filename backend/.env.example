# Example environment variables for AI-Technical-Interviewer backend
# Copy this file to .env and fill in real values before running in development or production.

# Resend (email) API key - required if you want email routes enabled
# Get a key from https://resend.com and paste it without quotes
RESEND_API_KEY=

# Optional: from email address used to send messages
FROM_EMAIL=noreply@example.com

# MongoDB connection URI - if omitted the app will use filesystem for storage
MONGO_URI=
MONGO_DB_NAME=

# Frontend base URLs (used in email links)
FRONTEND_URL=http://localhost:5173
PRODUCTION_FRONTEND_URL=

# Optional: port to run the backend
PORT=3000

# Other env vars used by the project
# OPENAI_API_KEY=your_openai_key_here
# Any other required variables can be added here
